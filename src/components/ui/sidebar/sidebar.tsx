import { Box, IconButton } from "@mui/material";

export default function Sidebar() {
  return (
    <Box sx={{ backgroundColor: "#1f1f1f", width: "100px", height: "100vh" }}>
      <IconButton></IconButton>
    </Box>
  );
}
